<h1>Order Detail</h1>

Order date/time: <%= @order.created_at %>
Purchaser name: <%= @user.full_name %>
Purchaser email: <%= @user.email %>

<% @order_items.each do |order_item| %>
  Item: <%= item.title %>
  Price: <%= item.price %>
  Quantity: <%= item.order_item.by_order(@order.id).quantity %>
  Item subtotal: <%= item.order_item.order_item.line_item_price %>
<% end %>

Order total:
Order status:
